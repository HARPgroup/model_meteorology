# to install this do the following
cd [your cbp model root where the source tree is] (ex: /opt/model/p6/vadeq)
. hspf_config
cd [back to this source tree] (ex: /opt/model/model_meteorology/prad_met)
make all
make install
